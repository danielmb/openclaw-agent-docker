# Copy this file to .env and fill in the values.
# cp .env.example .env

# ── Paths ──────────────────────────────────────────────────────────────────────
# Where openclaw config and workspace live on the host.
# Windows example:  C:/Users/YourName/.openclaw
# Linux/macOS:      /home/yourname/.openclaw
OPENCLAW_CONFIG_DIR=
OPENCLAW_WORKSPACE_DIR=

# ── Gateway token ──────────────────────────────────────────────────────────────
# Generated by the onboarding wizard (docker compose run --rm openclaw-cli onboard).
OPENCLAW_GATEWAY_TOKEN=

# ── Ports ──────────────────────────────────────────────────────────────────────
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# ── Image ──────────────────────────────────────────────────────────────────────
# Override to use a pre-built image (e.g. from a registry) instead of building locally.
# OPENCLAW_IMAGE=openclaw:local

# Pin to a specific Git branch/tag when building:
# OPENCLAW_REF=main

# Extra apt packages to bake into the image:
# OPENCLAW_DOCKER_APT_PACKAGES=ffmpeg build-essential

# ── AI provider credentials ────────────────────────────────────────────────────
CLAUDE_AI_SESSION_KEY=
CLAUDE_WEB_SESSION_KEY=
CLAUDE_WEB_COOKIE=
